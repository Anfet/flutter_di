name: simple_service_locator
description: Lightweight hierarchical dependency injection for Flutter with scoped lifecycles and tagged lookups.
version: 0.1.0
repository: https://github.com/Anfet/flutter_di
homepage: https://github.com/Anfet/flutter_di
issue_tracker: https://github.com/Anfet/flutter_di/issues
topics:
  - dependency-injection
  - flutter
  - ioc
  - scoped

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
